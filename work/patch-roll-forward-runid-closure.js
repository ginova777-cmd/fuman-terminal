const fs = require('fs');
const file = 'C:/fuman-terminal/scripts/run-terminal-auto-roll-forward.js';
let s = fs.readFileSync(file, 'utf8');
const anchor = `  if (state.includes("DISPLAY") || state.includes("DEGRADED") || state.includes("PREVIOUS")) {`;
const block = `  const isRunIdClosureState = state.includes("RUNID_MISMATCH") || String(base.nextAction || "").includes("refresh_terminal_snapshot_bundle_mobile_88_readback");\n  if (isRunIdClosureState) {\n    if (requiresProtectedReadbackCredential(base) && !protectedReadbackCredentialArmed()) {\n      base.executable = false;\n      base.executionGuard = "protected_readback_credential_not_armed";\n      base.commands.push(npmRun("verify:protected-readback-credential"));\n      base.notes.push("Protected runId closure readback cannot auto-execute until the member readback credential is armed; this is a manual secret repair, not a scanner retry.");\n      return finalizeAction(base);\n    }\n    const publishAllowed = policyDecision.scorecardPublishAllowed === true || ALLOW_DEGRADED_PUBLISH;\n    base.executable = publishAllowed;\n    base.executionGuard = publishAllowed ? "manifest_gated_scorecard_closure_publish" : "manifest_not_green_scorecard_closure_deferred";\n    base.commands.push(npmRun("manifest:daily-terminal-run"));\n    base.commands.push(npmRun("verify:daily-terminal-run-manifest"));\n    if (publishAllowed) {\n      base.commands.push(npmRun("scorecard:publish"));\n      base.commands.push(npmRun("snapshot:desktop"));\n      base.commands.push(npmRun("verify:terminal-resource-chain:unattended", ["--", \`--expected-date=${'${currentTradeDate()}'}\`]));\n    }\n    base.notes.push("RunId closure mismatch requires Manifest green before scorecard /88 publish; until then it is queued and previous-good remains degraded.");\n    return finalizeAction(base);\n  }\n\n`;
if (!s.includes(anchor)) throw new Error('display anchor not found');
s = s.replace(anchor, block + anchor);
const testAnchor = `    { name: "publish-deferred", job: { key: "scorecard", state: "PUBLISH_DEFERRED_MANIFEST_PENDING" }, expectedExecutable: false, expectedGuard: "manifest_pending_publish_deferred" },\n`;
const testInsert = testAnchor + `    { name: "runid-mismatch-deferred", job: { key: "strategy4", state: "BLOCKED_RUNID_MISMATCH", nextAction: "refresh_terminal_snapshot_bundle_mobile_88_readback", blocker: "runid_mismatch:scorecard88=old" }, expectedExecutable: false, expectedGuard: "manifest_not_green_scorecard_closure_deferred" },\n`;
if (!s.includes(testAnchor)) throw new Error('test anchor not found');
s = s.replace(testAnchor, testInsert);
fs.writeFileSync(file, s, 'utf8');
