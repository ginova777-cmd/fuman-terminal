#!/usr/bin/env node
"use strict";
const fs=require("fs"),path=require("path");
const ROOT=path.resolve(__dirname,"..");
const URL=(process.env.SUPABASE_URL||"https://cpmpfhbzutkiecccekfr.supabase.co").replace(/\/+$/,""), symbols=(process.argv.find(x=>x.startsWith("--symbols="))||"--symbols=2408,2455,3231").split("=")[1].split(",").map(x=>x.trim()).filter(x=>/^\d{4}$/.test(x));
function secret(n){for(const p of [path.join("C:\\fuman-runtime","secrets",n),path.join(ROOT,"secrets",n)]){try{const v=fs.readFileSync(p,"utf8").trim();if(v)return v}catch{}}return""}
function dateTW(){return new Intl.DateTimeFormat("en-CA",{timeZone:"Asia/Taipei",year:"numeric",month:"2-digit",day:"2-digit"}).format(new Date())}
async function main(){const key=process.env.SUPABASE_ANON_KEY||secret("supabase-anon-key.txt");if(!key)throw new Error("SUPABASE_ANON_KEY is required");const query=`v_fugle_intraday_5m_readback?select=trade_date,symbol,name,candle_time,open,high,low,close,volume,bar_count,source_status,updated_at,stale_seconds,bar_complete,data_gap_5m,source&trade_date=eq.${dateTW()}&symbol=in.(${symbols.join(",")})&order=candle_time.desc&limit=600`;const r=await fetch(`${URL}/rest/v1/${query}`,{headers:{apikey:key,Authorization:`Bearer ${key}`,Accept:"application/json"},signal:AbortSignal.timeout(15000)});const t=await r.text();if(!r.ok)throw new Error(`readback HTTP ${r.status}: ${t.slice(0,500)}`);const rows=JSON.parse(t),fail=[];for(const row of rows){if(Number(row.bar_count)<4&&row.source_status!=="DATA_GAP_5M")fail.push(`${row.symbol}:${row.candle_time}:gap_not_marked`);if(Number(row.bar_count)>=4&&row.source_status!=="ready")fail.push(`${row.symbol}:${row.candle_time}:ready_not_marked`)}const latest={};for(const row of rows)if(!latest[row.symbol])latest[row.symbol]=row;const out={ok:fail.length===0,contract:"daytrade_intraday_5m_readback_v1",trade_date:dateTW(),checked_at:new Date().toISOString(),symbols,rows:rows.length,data_gap_5m_count:rows.filter(x=>x.data_gap_5m).length,latest_by_symbol:latest,failed_checks:fail,formal_buy_authority:false};console.log(JSON.stringify(out,null,2));if(fail.length)process.exitCode=1}
main().catch(e=>{console.error(JSON.stringify({ok:false,error:e.message},null,2));process.exitCode=1});
