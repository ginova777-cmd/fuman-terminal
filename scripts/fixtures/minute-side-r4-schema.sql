CREATE ROLE anon; CREATE ROLE authenticated; CREATE ROLE service_role;
CREATE TABLE public.fugle_daytrade_minute_side_source_v1 (trade_date date,canonical_run_id text,symbol text,minute_start timestamptz,source_contract text,source_version text,inside_1m numeric,outside_1m numeric,unknown_1m numeric,total_1m numeric,volume_unit text,aggregation text,side_volume_timestamp timestamptz,source text,start_boundary_identity text,end_boundary_identity text,is_synthetic boolean,source_hash text,baseline_method text,baseline_sample_count integer,baseline_value numeric,outside_baseline_value numeric,inside_baseline_value numeric,raw_outside_ratio numeric,outside_strength numeric,raw_inside_ratio numeric,inside_strength numeric,dynamic_ratio numeric,outside_dynamic_ratio numeric,inside_dynamic_ratio numeric,side_state text,outside_baseline_sample_count integer,inside_baseline_sample_count integer,outside_side_state text,inside_side_state text,writer_run_id text,generation_id text,mother_pool_run_id text,snapshot_generation text,snapshot_sequence integer,created_at timestamptz NOT NULL DEFAULT now(),PRIMARY KEY(trade_date,canonical_run_id,symbol,minute_start,source_contract,source_version));
CREATE TABLE public.fugle_daytrade_minute_side_round_v1 (writer_run_id text,trade_date date,canonical_run_id text,generation_id text,mother_pool_run_id text,snapshot_generation text,snapshot_sequence integer,symbol text,requested boolean,written boolean,readback boolean,source_rows integer,data_gap_count integer,first_blocker text,minute_start timestamptz,source_hash text,created_at timestamptz NOT NULL DEFAULT now(),PRIMARY KEY(writer_run_id,symbol));
