function installMarketAiRuntimeLine(){const box="display:flex;flex-wrap:wrap;gap:10px;margin:0 0 14px;padding:10px 12px;border:1px solid rgba(127,166,255,.28);border-radius:8px;color:#b9c9e8;font-size:13px;font-weight:800",run=()=>{try{const panel=document.querySelector("#market-view"),anchor=panel?.querySelector(".market-ai-summary,.market-ai-block");if(!anchor)return;let line=panel.querySelector(".market-ai-runtime-line");line||(line=document.createElement("div"),line.className="market-ai-runtime-line",line.style.cssText=box,anchor.before(line));const now=new Date,mins=60*now.getHours()+now.getMinutes(),active=mins>=540&&mins<=810,label=active?"運作中":"盤後模式",note=active?"盤中資料同步判讀":"收盤/快取資料判讀，不新增即時訊號";line.innerHTML=`<strong>AI 判讀運作時間：09:00-13:30</strong><span>${label}</span><span>${note}</span>`}catch(error){}};setInterval(run,1500),document.addEventListener("click",event=>{event.target.closest?.("[data-market-mode]")&&setTimeout(run,120)},!0),setTimeout(run,700)}function installMarketAiPriorityRiskGuard(){const items=[["事件波動風險最高","6/17 台指期大結算、6/19 美股四巫日接近；留意尾盤結算、轉倉與避險。"],["個股極端波動風險","接近 +10% 或 -10% 僅標方向，不當追買；先確認量、族群、隔日開盤。"],["AI 盤中/盤後模式風險","09:00-13:30 盤中判讀；盤後只做隔日風險提醒，不當秒級下單訊號。"]],run=()=>{try{const block=[...document.querySelectorAll("#market-view .market-ai-block")].find(node=>(node.querySelector("h3")?.textContent||"").includes("風險提醒")),risk=block?.querySelector(".market-ai-risk");if(!risk)return;risk.querySelectorAll("[data-ai-priority-risk]").forEach(node=>node.remove()),items.slice().reverse().forEach(([title,text])=>{const article=document.createElement("article");article.dataset.aiPriorityRisk="1",article.innerHTML=`<h4>${title}</h4><p>${text}</p>`,risk.prepend(article)});const count=block.querySelector("small");count&&(count.textContent="3 則優先風險 + 原始提醒")}catch(error){}};setInterval(run,1500),document.addEventListener("click",event=>{event.target.closest?.("[data-market-mode]")&&setTimeout(run,120)},!0),setTimeout(run,700)}function installMarketAiLiveContractPanel(){if(window.__fumanMarketAiLiveContractPanel)return;window.__fumanMarketAiLiveContractPanel="20260630-01";const endpoint="/api/market-ai-live?canvas=1&compact=1&shell=1&limit=40",esc=value=>String(value??"").replace(/[&<>"']/g,ch=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"}[ch])),arr=value=>Array.isArray(value)?value:[],num=value=>Number(value)||0,dateLabel=value=>{const text=String(value||"").replace(/\D/g,"");return text.length>=8?`${text.slice(4,6)}/${text.slice(6,8)}`:"--"};let loading=!1,lastSig="";function aiPanel(){return document.querySelector("#market-view .market-ai-panel,#market-view [data-market-api-ai],#market-ai-panel")}function active(){const view=document.querySelector("#market-view");return Boolean(view?.classList.contains("active")&&document.querySelector('[data-market-mode="ai"]')?.classList.contains("active"))}function row(stock,index){return`<article class="market-ai-stock-row"><div class="market-ai-rank">#${index+1}</div><div><h4><span class="market-ai-code">${esc(stock.code||"--")}</span><span class="market-ai-name">${esc(stock.name||"--")}</span></h4><p>${esc(stock.source||"AI 判讀")}，分數 ${esc(stock.score??"--")}，漲幅 ${num(stock.pct??stock.percent).toFixed(2)}%。</p></div><div><span class="market-ai-chip">${esc(stock.industry||"--")}</span></div><div class="market-ai-score"><small>分數</small><strong>${esc(stock.score??"--")}</strong></div><div class="market-ai-tags">${arr(stock.tags).slice(0,5).map(tag=>`<span>${esc(tag)}</span>`).join("")}</div></article>`}function paint(payload){const panel=aiPanel();if(!panel)return;const dashboard=payload?.dashboard||{},fresh=payload?.dataFreshness||{},summary=payload?.summary||{},issues=arr(fresh.sourceIssues),stale=arr(fresh.staleSources),points=arr(payload?.todayPoints),risks=arr(payload?.riskNotes),reasoning=arr(payload?.reasoning),stocks=arr(payload?.hotStocks),tradeDate=dashboard.tradeDate||fresh.heatmapTradeDate||fresh.radarTradeDate||fresh.today||payload?.marketSession?.today,status=issues.length?`水源異常：${issues.join("；")}`:stale.length?`舊資料已排除：${stale.join("、")}`:"今日正式水源可用",sig=JSON.stringify([payload?.updatedAt,tradeDate,dashboard.sample,dashboard.up,dashboard.down,dashboard.bias,status,stocks.map(s=>`${s.code}:${s.score}`).join("|")]);if(sig===lastSig&&panel.dataset.marketApiAi==="live-contract")return;lastSig=sig;panel.dataset.marketApiAi="live-contract";panel.innerHTML=`<section class="market-ai-summary"><article class="market-ai-card hero"><small>盤中決策總覽 · 資料 ${esc(dateLabel(tradeDate))}</small><strong>${esc(dashboard.bias||"等待方向")}</strong><p>${esc(status)}。${esc(payload?.priorityObservation?.text||"等待正式水源判讀。")}</p><div class="market-ai-metrics"><span>樣本數<b>${num(dashboard.sample||summary.sample).toLocaleString("zh-TW")}</b></span><span>上漲<b>${num(dashboard.up||summary.up).toLocaleString("zh-TW")}</b></span><span>下跌<b>${num(dashboard.down||summary.down).toLocaleString("zh-TW")}</b></span><span>信心<b>${esc(dashboard.confidence||summary.confidence||"觀察")}</b></span></div></article><article class="market-ai-card"><small>操作建議</small><strong>${esc(dashboard.action||"等待方向")}</strong><p>${esc(payload?.priorityObservation?.text||"依正式 API 判讀，不讀舊 panel JSON。")}</p></article><article class="market-ai-card ${issues.length?"warning":""}"><small>水源狀態</small><strong>${issues.length?"水源異常":"水源正常"}</strong><p>${esc(status)}</p></article><article class="market-ai-card"><small>正式 contract</small><strong>${esc(payload?.source||payload?.cacheSource||"api/market-ai-live")}</strong><p>heatmap ${esc(fresh.heatmapTradeDate||"--")} / radar ${esc(fresh.radarTradeDate||"--")}；舊 snapshot 不作正常判讀。</p></article></section><section class="market-ai-main"><article class="market-ai-block"><h3>AI 今日重點</h3><small>${esc(dateLabel(tradeDate))} 最新資料</small><div class="market-ai-list">${(points.length?points:[status]).slice(0,5).map((text,index)=>`<div class="market-ai-point"><b>${index+1}</b><span>${esc(text)}</span></div>`).join("")}</div></article><aside class="market-ai-block"><h3>風險提醒</h3><small>${risks.length} 則</small><div class="market-ai-risk">${(risks.length?risks:[{title:issues.length?"水源異常":"水源正常",text:status}]).slice(0,4).map(note=>`<article><h4>${esc(note.title||"風險")}</h4><p>${esc(note.text||note.reason||"")}</p></article>`).join("")}</div></aside></section><section class="market-ai-block"><h3>AI 判讀依據</h3><small>正式 API-only contract</small><div class="market-ai-evidence">${reasoning.slice(0,5).map(item=>`<article><small>${esc(item.key||"依據")}</small><strong>${esc(item.title||"--")}</strong><p>${esc(item.text||"")}</p></article>`).join("")||`<article><small>來源</small><strong>${esc(payload?.source||"api/market-ai-live")}</strong><p>${esc(status)}</p></article>`}</div></section><section class="market-ai-block"><h3>熱門觀察股</h3><small>依正式 API 排序</small><div class="market-ai-hot">${stocks.length?stocks.slice(0,10).map(row).join(""):'<div class="empty-state">目前正式 API 尚未產生觀察股。</div>'}</div></section>`}async function run(force=false){if(loading||!active())return;const panel=aiPanel();if(!panel)return;loading=!0;try{const response=await fetch(`${endpoint}&t=${Date.now()}`,{cache:"no-store",headers:{"Cache-Control":"no-store"}});if(!response.ok)throw new Error(`market_ai_live_http_${response.status}`);paint(await response.json())}catch(error){panel.innerHTML=`<div class="empty-state">AI 判讀正式水源讀取失敗：${esc(error?.message||error)}。不使用舊 panel cache 當正常資料。</div>`}finally{loading=!1}}setInterval(()=>run(false),3000);document.addEventListener("click",event=>{event.target.closest?.("[data-market-mode],[data-view]")&&setTimeout(()=>run(true),250)},!0);document.addEventListener("visibilitychange",()=>{document.hidden||run(true)},{passive:!0});window.addEventListener("focus",()=>run(true),{passive:!0});setTimeout(()=>run(true),1200)}installMarketAiRuntimeLine();installMarketAiPriorityRiskGuard();installMarketAiLiveContractPanel();
