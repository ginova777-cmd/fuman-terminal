(function () {
  window.FUMAN_TUNING_CONFIG = Object.freeze({
    realtimeRadarFreshMs: 2 * 60 * 1000,
    realtimeRadarStaleMs: 5 * 60 * 1000,
    realtimeRadarLastCacheKey: "fuman_realtime_radar_last_rows_v2",
    realtimeRadarHistoryTargetLimit: 72,
    realtimeRadarHistoryBatchSize: 12,
    realtimeRadarHistoryRefreshMs: 10 * 60 * 1000,
    chipTradeCacheMs: 10 * 60 * 1000,
    chipWarrantActiveRefreshMs: 60 * 1000,
    strategy4LocalCacheKey: "fuman_strategy4_scan_cache_v1",
    strategy4BackupCacheKey: "fuman_strategy4_nonempty_backup_v1",
    openBuyLocalCacheKey: "fuman_open_buy_scan_cache_v1",
    openBuyBackupCacheKey: "fuman_open_buy_nonempty_backup_v1",
    exportUnlockKey: "fuman_export_unlock_until_v1",
    exportUnlockMs: 30 * 24 * 60 * 60 * 1000,
    warrantFlowLocalCacheKey: "fuman_warrant_flow_cache_v1",
    cacheFreshMs: 10 * 60 * 1000,
    marketRefreshLiveMs: 5 * 1000,
    marketRefreshClosedMs: 10 * 60 * 1000,
    marketRefreshHiddenMs: 5 * 60 * 1000,
    marketHeatmapClosedMs: 10 * 60 * 1000,
    marketPollTickMs: 5 * 1000,
    marketDomRefreshMs: 60 * 1000,
    watchlistRefreshLiveMs: 30 * 1000,
    watchlistRefreshClosedMs: 120 * 1000,
    watchlistRefreshHiddenMs: 180 * 1000,
    intradayHotScanLimit: 900,
    realtimeRadarPoolLimit: 650,
    intradayBackgroundBatch: 450,
    intradayFastScanMs: 3000,
    intradayBackgroundScanMs: 3000,
    realtimeRadarRefreshMs: 3000,
    mobileIntradayHotScanLimit: 260,
    mobileIntradayForceExtraLimit: 80,
    mobileIntradayBackgroundBatch: 90,
    mobileIntradayHotScanMs: 12000,
    mobileIntradayBackgroundScanMs: 45000,
    mobileOtherStrategyRenderMs: 2500,
    mobileOtherStrategyCacheMs: 45000,
    intradayCandidateTtlMs: 15 * 60 * 1000,
    intradayMinVolume: 2000,
    strategy2IntradayMinDisplayPct: 2,
    terminalPageSize: 10,
    terminalPageSizeOptions: [10, 20, 50],
    terminalPageSizes: {
      swing: 50,
      openBuy: 20,
      strategy3: 20,
      strategy5: 20,
      warrant: 20,
      chip: 20,
    },
  });
})();
