{
  "name": "fuman-terminal",
  "version": "0.1.0",
  "private": true,
  "description": "Fuman stock terminal with a Vercel API proxy for official market data.",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod",
    "verify:deploy": "node --use-system-ca scripts/verify-deploy.js"
  },
  "engines": {
    "node": "20.x"
  }
}
