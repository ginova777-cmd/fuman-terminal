"use strict";
const path=require("path");
function normalizeSymbol(value){const symbol=String(value?.symbol||value?.code||value||"").replace(/\D/g,"").slice(0,4);return /^\d{4}$/.test(symbol)?symbol:"";}
function loadStrategy3MotherPool({runtimeDir,tradeDate,readJson}){const file=path.join(runtimeDir,"cache","intraday","fugle-daytrade-ws-priority-symbols.json");const payload=readJson(file,null);const payloadTradeDate=String(payload?.tradeDate||"").slice(0,10);const raw=Array.isArray(payload?.daytradeMotherPoolSymbols)?payload.daytradeMotherPoolSymbols:Array.isArray(payload?.strategy2FormalWaterSymbols)?payload.strategy2FormalWaterSymbols:[];const symbols=[...new Set(raw.map(normalizeSymbol).filter(Boolean))];const source=Array.isArray(payload?.daytradeMotherPoolSymbols)?"daytradeMotherPoolSymbols":"strategy2FormalWaterSymbols";const issues=[];if(!payload)issues.push("strategy3_mother_pool_cache_missing");if(payloadTradeDate!==tradeDate)issues.push("strategy3_mother_pool_trade_date_mismatch");if(!symbols.length)issues.push("strategy3_mother_pool_symbols_empty");return{ok:issues.length===0,file,tradeDate:payloadTradeDate,expectedTradeDate:tradeDate,source,symbols,expectedCount:symbols.length,issues};}
module.exports={loadStrategy3MotherPool,normalizeSymbol};
